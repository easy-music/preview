---
title: "tracks API - v1"
service: "tracks"
version: "v1"
status: "draft"
created_by: "ci"
created_at: "2025-09-14T20:26:53+03:00"
changelog: |
- auto-generated from OpenAPI
---

![status](https://img.shields.io/badge/status-draft-yellow)

---
title: Tracks Service v1.0.0
language_tabs:
  - shell: Shell
  - http: HTTP
  - javascript: JavaScript
  - ruby: Ruby
  - python: Python
  - php: PHP
  - java: Java
  - go: Go
toc_footers: []
includes: []
search: true
highlight_theme: darkula
headingLevel: 2

---

<!-- Generator: Widdershins v4.0.1 -->

<h1 id="tracks-service">Tracks Service v1.0.0</h1>

> Scroll down for code samples, example requests and responses. Select a language for code samples from the tabs above or the mobile navigation menu.

<h1 id="tracks-service-default">Default</h1>

## get__v1_tracks_{track_id}_stream

> Code samples

```shell
# You can also use wget
curl -X GET /v1/tracks/{track_id}/stream \
  -H 'Accept: application/json'

```

```http
GET /v1/tracks/{track_id}/stream HTTP/1.1

Accept: application/json

```

```javascript

const headers = {
  'Accept':'application/json'
};

fetch('/v1/tracks/{track_id}/stream',
{
  method: 'GET',

  headers: headers
})
.then(function(res) {
    return res.json();
}).then(function(body) {
    console.log(body);
});

```

```ruby
require 'rest-client'
require 'json'

headers = {
  'Accept' => 'application/json'
}

result = RestClient.get '/v1/tracks/{track_id}/stream',
  params: {
  }, headers: headers

p JSON.parse(result)

```

```python
import requests
headers = {
  'Accept': 'application/json'
}

r = requests.get('/v1/tracks/{track_id}/stream', headers = headers)

print(r.json())

```

```php
<?php

require 'vendor/autoload.php';

$headers = array(
    'Accept' => 'application/json',
);

$client = new \GuzzleHttp\Client();

// Define array of request body.
$request_body = array();

try {
    $response = $client->request('GET','/v1/tracks/{track_id}/stream', array(
        'headers' => $headers,
        'json' => $request_body,
       )
    );
    print_r($response->getBody()->getContents());
 }
 catch (\GuzzleHttp\Exception\BadResponseException $e) {
    // handle exception or api errors.
    print_r($e->getMessage());
 }

 // ...

```

```java
URL obj = new URL("/v1/tracks/{track_id}/stream");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

```go
package main

import (
       "bytes"
       "net/http"
)

func main() {

    headers := map[string][]string{
        "Accept": []string{"application/json"},
    }

    data := bytes.NewBuffer([]byte{jsonReq})
    req, err := http.NewRequest("GET", "/v1/tracks/{track_id}/stream", data)
    req.Header = headers

    client := &http.Client{}
    resp, err := client.Do(req)
    // ...
}

```

`GET /v1/tracks/{track_id}/stream`

*Get streaming URL for a track*

<h3 id="get__v1_tracks_{track_id}_stream-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|track_id|path|string|true|none|
|quality|query|string|false|none|

#### Enumerated Values

|Parameter|Value|
|---|---|
|quality|auto|
|quality|low|
|quality|medium|
|quality|high|

> Example responses

> 200 Response

```json
{
  "track_id": "string",
  "type": "string",
  "url": "http://example.com",
  "expires_at": "2019-08-24T14:15:22Z"
}
```

<h3 id="get__v1_tracks_{track_id}_stream-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|streaming URL|Inline|

<h3 id="get__v1_tracks_{track_id}_stream-responseschema">Response Schema</h3>

Status Code **200**

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|» track_id|string|false|none|none|
|» type|string|false|none|none|
|» url|string(uri)|false|none|none|
|» expires_at|string(date-time)|false|none|none|

<aside class="success">
This operation does not require authentication
</aside>

